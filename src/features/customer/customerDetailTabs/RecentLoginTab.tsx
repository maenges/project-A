import { Typography } from '@mui/material';

const RecentLoginTab = () => (
  <Typography sx={(theme) => ({ color: theme.palette.text.secondary })}>
    최근 로그인 기록 콘텐츠
  </Typography>
);

export default RecentLoginTab;
