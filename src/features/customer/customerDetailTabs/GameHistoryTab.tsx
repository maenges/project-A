import { Typography } from '@mui/material';

const GameHistoryTab = () => (
  <Typography sx={(theme) => ({ color: theme.palette.text.secondary })}>게임이력 콘텐츠</Typography>
);

export default GameHistoryTab;
